/// {@template meno_tab_type}
/// Defines the visual style of the tab bar.
/// {@endtemplate}
enum MenoTabType {
  /// The normal tab style, typically with an underline indicator.
  normal,

  /// The contained tab style, where each tab appears as a separate contained
  /// element.
  contained,
}
